#include <iostream>
using namespace std;

struct node {
	int data;
	node* next;
};

int main()
{
	node* L = NULL;
	/* TODO: try to actually write the code from class today to add new
	 * elements to the beginning of a list and to print the contents of
	 * a list.  */
}

// vim:foldlevel=2
